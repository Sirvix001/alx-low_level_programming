#include <stdio.h>

/**
 * main - check that prints base 16 all in lowercase alphabets
 *
 * Return: Always 0 (successful)
 */

int main(void)
{
char c;
for (c = '0'; c <= '0'; c++)
{
putchar(c);
}
for (c = 'a'; c <= 'f'; c++)
{
putchar(c);
}
putchar('\n');
return (0);
}
